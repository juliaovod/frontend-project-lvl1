name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node: [13.x]

    steps:
      - name: Install dependencies
        run: make install

      - name: Run linter
        run: make lint
